name: Build Botsafe JS

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-js:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build TypeScript
        run: npm run build

      - name: Commit JS
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add Scripts/_botsafe.js
          git commit -m "Auto-build _botsafe.js from TS" || echo "No changes"
          git push
